# Changelog

_Generated from Conventional Commits by `npm run changelog` (`tools/dev-metrics.js`). Not a CI gate; regenerate as needed._

20 commits (2026-05-24 → 2026-05-24).

## Other
- Add anti-patterns playbook, dev-workflow tooling, templates; wire new modules (`793d5b1`)
- Add safe dynamic SQL helper (anti-injection) (`22b015d`)
- Add feature-flag service (decouple deploy from release) (`6207fa9`)
- Add BDC->BAPI/RAP cookbook and CI-published generated API reference (`563cef6`)
- Add data-export cookbook; wire wrapper + export modules into catalog/matrix (`f2731d3`)
- Add data-export generator: report/CDS -> OData (Power BI), cube, extraction (`feddd80`)
- Add Clean Core released-wrapper generator (ZCL_AU_WRAP_GEN) (`23d79c2`)
- Extend Fiori generator: value help, metadata extension, read-only option (`05f75be`)
- Add Fiori conversion cookbook; wire Fiori module into catalog/matrix/docs (`09569ae`)
- Add Fiori scaffolding generator (table/ALV -> CDS+RAP+service) (`14bc041`)
- Add related-projects map pointing to established dotabap projects (`fa3455d`)
- Make cloud-safe modules genuinely clean-core; add readiness matrix (`d2eeef0`)
- Add RAP/CDS/BTP modernization guide, expand cookbooks, add ARCHITECTURE (`191a8ac`)
- Add Clock, Retry and Guard utilities (cross-language patterns) (`2a915db`)
- Fix generic-table compile bug in count_distinct; harden config; add comments (`1283f63`)
- Add app-server I/O, locking, jobs, config, hashing, zip, timer, context, docgen (`978085a`)
- Add Clean Core helpers, ALV events, cookbooks and local AI review (`f28103e`)
- Add ALV (SALV), email, HTTP and number-range utilities (`941e3e2`)
- Add plug-and-play ABAP & RAP utility library (`49f67ad`)
- Initialize repository (`0e84ad6`)

